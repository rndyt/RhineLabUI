# RhineLabUI 博客改造验证

2026-09-10，分支 `codex/rhine-blog`。

## 已验证

- `npm run build`：TypeScript 编译、Vite 构建、PWA 离线清单生成通过。
- `npm run check:content`：13 项测试通过。覆盖 3 / 2 / 2 不等长分类、正反向循环、负坐标、跨 1000 个内容周期、稳定编号、非法内容拒绝、Markdown HTML 清理、独立页面与 TXT 输出一致性。
- `npm run check:viewport`：既有视口、参考取景和渲染分辨率检查通过。
- 浏览器实际打开三维阵列，工程文章（三篇）→ AI 实践（两篇），编号、刻度与分类计数正确。
- 点击读取后显示玻璃模型与博客正文，URL 带文章 slug，页面标题随文章更新。
- 直接打开 `/?post=ai-workflow-placeholder`，定位到 X-004 并进入详情。
- Markdown 目录显示七个小节；点击最后一节后返回正文并滚动到该小节。
- 点击“展开阅读”进入独立 HTML 文章，正文、代码块、目录、相关阅读和三维返回链接均可见。
- 390×844 浏览器视口检查：上方模型、下方正文；正文容器可独立滚动，页面宽度为 390，无横向页面溢出。临时视口已恢复。
- 保留原模型文件与材质/镜头/波浪参数；仅改模型标签文字与内容映射。

## 边界

手机检查为桌面浏览器的视口模拟，不是 iPhone 实机结果。本次没有重做上游的完整音频试听、离线安装或像素级视频比对；其原有实现被保留。当前内容仍是七篇示例，保持全站 noindex。

本地预览为 `http://127.0.0.1:5174/`。改造未合并、未推送，也没有部署到原项目正式域名。
